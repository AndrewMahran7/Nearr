# Token UX screenshot capture

Automated visual capture was unavailable on this Windows host on 2026-09-03:

- The configured Android API 36.1 emulator remained `offline` after both a
  normal start and a cold boot, so the app could not be installed or captured.
- The in-app browser runtime reported no available browser instances.
- Expo web bundling is not a supported project target and failed in the
  React Native compatibility layer after a local-only preview attempt.

No generated image is presented as simulator proof. Founder physical QA must
capture these five states from the published Nearr-Dev OTA:

1. Settings token card.
2. Token store with Explorer Pack selected.
3. Zero-balance pending-video paywall.
4. Purchase success with the new numeric balance and token symbol.
5. Map with no token balance visible.
